database=petclinic
spring.datasource.url=${POSTGRES_URL:jdbc:postgresql://sprinc-petclinic.postgres.database.azure.com/petclinic?sslmode=require}
spring.datasource.username=${POSTGRES_USER:sergioferal14}
spring.datasource.password=${POSTGRES_PASS:*******}
# SQL is written to be idempotent so this is safe
spring.sql.init.mode=always